apiVersion: v1
kind: Service
metadata:
  name: kaps-redis
  namespace: kaps-ai
spec:
  selector:
    app: kaps-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
