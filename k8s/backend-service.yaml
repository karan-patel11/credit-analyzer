apiVersion: v1
kind: Service
metadata:
  name: kaps-backend
  namespace: kaps-ai
spec:
  selector:
    app: kaps-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
