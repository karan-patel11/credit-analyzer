apiVersion: v1
kind: Service
metadata:
  name: kaps-frontend
  namespace: kaps-ai
spec:
  selector:
    app: kaps-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
